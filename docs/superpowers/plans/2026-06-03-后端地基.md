# 后端地基 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建可运行、可测试的 FastAPI 后端地基，打通微信登录 + JWT 认证全链路。

**Architecture:** 领域驱动模块化（modules/ + common/），SQLAlchemy 2.0 同步 ORM + MySQL，Redis 缓存，python-jose 签发 JWT，httpx 调用微信 code2session。单元测试用 SQLite 内存库 + mock 微信接口，无需依赖运行中的 MySQL/微信服务。

**Tech Stack:** Python 3.11、FastAPI、SQLAlchemy 2.0（同步 + PyMySQL）、Alembic、redis-py、python-jose、pydantic-settings、httpx、pytest、Docker Compose、Nginx

**范围说明:** 本计划仅覆盖后端地基。前端 Taro 脚手架是独立的下一份计划。

**决策记录:**
- **同步 ORM 而非异步:** MVP <1000 用户，同步 SQLAlchemy 更简单可靠，避免异步驱动复杂度（YAGNI）。
- **测试用 SQLite 内存库:** 单元测试不依赖真实 MySQL，跑得快。JSON 列在 SQLite 与 MySQL 上均兼容。
- **JWT 用 python-jose（HS256）:** access token 7 天，refresh token 30 天。

---

## 文件结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 入口 + 路由注册 + 健康检查
│   ├── config.py               # pydantic-settings 配置
│   ├── common/
│   │   ├── __init__.py
│   │   ├── database.py         # SQLAlchemy engine / session / Base
│   │   ├── redis_client.py     # Redis 连接
│   │   ├── response.py         # 统一响应 {code,message,data,timestamp}
│   │   ├── exceptions.py       # 业务异常 + 全局异常处理器
│   │   └── auth.py             # JWT 签发/校验 + get_current_user 依赖
│   └── modules/
│       └── user/
│           ├── __init__.py
│           ├── models.py       # User 表
│           ├── schemas.py      # 登录请求/响应模型
│           ├── service.py      # 微信登录业务逻辑
│           └── router.py       # /user/auth/login 等路由
├── alembic/                    # 迁移目录（alembic init 生成）
├── alembic.ini
├── tests/
│   ├── __init__.py
│   ├── conftest.py             # pytest fixtures（DB / client）
│   ├── test_response.py
│   ├── test_auth.py
│   └── test_user_login.py
├── requirements.txt
├── .env.example
└── Dockerfile

docker/
├── docker-compose.yml
└── nginx/nginx.conf
```

---

### Task 0: 项目骨架与依赖

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/.env.example`
- Create: `backend/app/__init__.py`
- Create: `backend/app/config.py`
- Create: `backend/app/main.py`
- Create: `backend/tests/__init__.py`
- Create: `backend/tests/conftest.py`

- [ ] **Step 1: 写依赖清单**

Create `backend/requirements.txt`（用 `>=` 版本下限，兼容 Python 3.11 与 3.13；本地开发为 3.13，Docker 镜像为 3.11）:

```
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.35
pymysql>=1.1.0
alembic>=1.13.0
redis>=5.0.1
python-jose[cryptography]>=3.3.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
pytest>=8.0.0
pytest-mock>=3.12.0
```

- [ ] **Step 2: 安装依赖**

Run: `cd backend && python -m venv .venv && source .venv/Scripts/activate && pip install -r requirements.txt`
（Windows Git Bash 用 `.venv/Scripts/activate`；Linux/Mac 用 `.venv/bin/activate`）
Expected: 所有包安装成功，无报错

- [ ] **Step 3: 写配置文件**

Create `backend/app/config.py`:

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    # 数据库
    database_url: str = "mysql+pymysql://root:root@localhost:3306/meishi?charset=utf8mb4"
    # Redis
    redis_url: str = "redis://localhost:6379/0"
    # JWT
    jwt_secret: str = "dev-secret-change-in-production"
    jwt_algorithm: str = "HS256"
    access_token_expire_days: int = 7
    refresh_token_expire_days: int = 30
    # 微信
    wechat_appid: str = ""
    wechat_secret: str = ""


settings = Settings()
```

Create `backend/.env.example`:

```
DATABASE_URL=mysql+pymysql://root:root@mysql:3306/meishi?charset=utf8mb4
REDIS_URL=redis://redis:6379/0
JWT_SECRET=change-this-to-a-random-string
WECHAT_APPID=your_appid
WECHAT_SECRET=your_secret
```

- [ ] **Step 4: 写最小 FastAPI 入口 + 健康检查**

Create `backend/app/__init__.py`:

```python
```
（空文件）

Create `backend/app/main.py`:

```python
from fastapi import FastAPI

app = FastAPI(title="AI健康生活助手 API")


@app.get("/health")
def health():
    return {"status": "ok"}
```

Create `backend/tests/__init__.py`（空文件）。

- [ ] **Step 5: 写测试 fixture**

Create `backend/tests/conftest.py`:

```python
import pytest
from fastapi.testclient import TestClient
from app.main import app


@pytest.fixture
def client():
    return TestClient(app)
```

- [ ] **Step 6: 写健康检查测试**

Create `backend/tests/test_response.py`:

```python
def test_health(client):
    resp = client.get("/health")
    assert resp.status_code == 200
    assert resp.json() == {"status": "ok"}
```

- [ ] **Step 7: 运行测试**

Run: `cd backend && python -m pytest tests/test_response.py -v`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add backend/
git commit -m "feat(backend): 项目骨架、配置与健康检查端点"
```

---

### Task 1: 统一响应与异常处理

**Files:**
- Create: `backend/app/common/__init__.py`
- Create: `backend/app/common/response.py`
- Create: `backend/app/common/exceptions.py`
- Test: `backend/tests/test_response.py`（追加）

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_response.py`:

```python
from app.common.response import success, fail


def test_success_response():
    r = success({"foo": "bar"})
    assert r["code"] == 0
    assert r["message"] == "success"
    assert r["data"] == {"foo": "bar"}
    assert isinstance(r["timestamp"], int)


def test_fail_response():
    r = fail(40001, "登录失败")
    assert r["code"] == 40001
    assert r["message"] == "登录失败"
    assert r["data"] is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_response.py -v`
Expected: FAIL（`ModuleNotFoundError: app.common.response`）

- [ ] **Step 3: 实现统一响应**

Create `backend/app/common/__init__.py`（空文件）。

Create `backend/app/common/response.py`:

```python
import time
from typing import Any


def _ts() -> int:
    return int(time.time() * 1000)


def success(data: Any = None) -> dict:
    return {"code": 0, "message": "success", "data": data, "timestamp": _ts()}


def fail(code: int, message: str, data: Any = None) -> dict:
    return {"code": code, "message": message, "data": data, "timestamp": _ts()}
```

- [ ] **Step 4: 实现业务异常与全局处理器**

Create `backend/app/common/exceptions.py`:

```python
from fastapi import Request
from fastapi.responses import JSONResponse
from app.common.response import fail


class BusinessError(Exception):
    def __init__(self, code: int, message: str):
        self.code = code
        self.message = message


def register_exception_handlers(app):
    @app.exception_handler(BusinessError)
    async def business_error_handler(request: Request, exc: BusinessError):
        return JSONResponse(status_code=200, content=fail(exc.code, exc.message))
```

- [ ] **Step 5: 在 main.py 注册处理器**

Modify `backend/app/main.py`:

```python
from fastapi import FastAPI
from app.common.exceptions import register_exception_handlers

app = FastAPI(title="AI健康生活助手 API")
register_exception_handlers(app)


@app.get("/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_response.py -v`
Expected: PASS（3 项全过）

- [ ] **Step 7: 提交**

```bash
git add backend/app/common backend/app/main.py backend/tests/test_response.py
git commit -m "feat(backend): 统一响应格式与业务异常处理"
```

---

### Task 2: 数据库层与 User 模型

**Files:**
- Create: `backend/app/common/database.py`
- Create: `backend/app/modules/__init__.py`
- Create: `backend/app/modules/user/__init__.py`
- Create: `backend/app/modules/user/models.py`
- Test: `backend/tests/test_user_login.py`（建文件，先放模型测试）

- [ ] **Step 1: 写失败测试**

Create `backend/tests/test_user_login.py`:

```python
from app.modules.user.models import User


def test_user_model_fields():
    u = User(openid="oABC123", nickname="小明")
    assert u.openid == "oABC123"
    assert u.nickname == "小明"
    assert u.is_member is False or u.is_member is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_user_login.py -v`
Expected: FAIL（`ModuleNotFoundError: app.modules.user.models`）

- [ ] **Step 3: 实现数据库基础**

Create `backend/app/common/database.py`:

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, DeclarativeBase
from app.config import settings


class Base(DeclarativeBase):
    pass


engine = create_engine(settings.database_url, pool_pre_ping=True, pool_recycle=3600)
SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 4: 实现 User 模型**

Create `backend/app/modules/__init__.py`（空文件）。
Create `backend/app/modules/user/__init__.py`（空文件）。

Create `backend/app/modules/user/models.py`:

```python
from datetime import datetime
from sqlalchemy import String, Boolean, DateTime, BigInteger
from sqlalchemy.orm import Mapped, mapped_column
from app.common.database import Base


class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
    openid: Mapped[str] = mapped_column(String(64), unique=True, index=True)
    unionid: Mapped[str | None] = mapped_column(String(64), nullable=True)
    nickname: Mapped[str | None] = mapped_column(String(64), nullable=True)
    avatar_url: Mapped[str | None] = mapped_column(String(255), nullable=True)
    is_member: Mapped[bool] = mapped_column(Boolean, default=False)
    member_expire_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, default=datetime.utcnow, onupdate=datetime.utcnow
    )
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_user_login.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/common/database.py backend/app/modules backend/tests/test_user_login.py
git commit -m "feat(backend): 数据库层与 User 模型"
```

---

### Task 3: Alembic 迁移

**Files:**
- Create: `backend/alembic.ini`、`backend/alembic/`（由 alembic init 生成）
- Modify: `backend/alembic/env.py`
- Create: 首个迁移脚本（自动生成）

- [ ] **Step 1: 初始化 alembic**

Run: `cd backend && alembic init alembic`
Expected: 生成 `alembic.ini` 和 `alembic/` 目录

- [ ] **Step 2: 配置 env.py 使用项目配置与模型**

Modify `backend/alembic/env.py` —— 在文件顶部 import 区之后加入，并替换 target_metadata：

```python
from app.config import settings
from app.common.database import Base
from app.modules.user.models import User  # noqa: F401 确保模型被注册

config.set_main_option("sqlalchemy.url", settings.database_url)
target_metadata = Base.metadata
```

- [ ] **Step 3: 生成首个迁移**

Run: `cd backend && alembic revision --autogenerate -m "create users table"`
Expected: 在 `alembic/versions/` 生成迁移脚本，包含 `create_table('users', ...)`

- [ ] **Step 4: 人工核对迁移脚本**

打开生成的迁移文件，确认 `upgrade()` 包含 users 表的全部字段（openid 有 unique 索引）。

- [ ] **Step 5: 提交**

```bash
git add backend/alembic.ini backend/alembic
git commit -m "feat(backend): alembic 迁移与 users 建表脚本"
```

---

### Task 4: Redis 层

**Files:**
- Create: `backend/app/common/redis_client.py`
- Test: `backend/tests/test_redis.py`

- [ ] **Step 1: 写测试（用 mock，不依赖真实 Redis）**

Create `backend/tests/test_redis.py`:

```python
from unittest.mock import MagicMock
from app.common import redis_client


def test_get_redis_returns_client(mocker):
    fake = MagicMock()
    mocker.patch.object(redis_client, "_pool", fake)
    client = redis_client.get_redis()
    assert client is not None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_redis.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 Redis 连接**

Create `backend/app/common/redis_client.py`:

```python
import redis
from app.config import settings

_pool = redis.ConnectionPool.from_url(settings.redis_url, decode_responses=True)


def get_redis() -> redis.Redis:
    return redis.Redis(connection_pool=_pool)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_redis.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/common/redis_client.py backend/tests/test_redis.py
git commit -m "feat(backend): Redis 连接层"
```

---

### Task 5: JWT 工具

**Files:**
- Create: `backend/app/common/auth.py`
- Test: `backend/tests/test_auth.py`

- [ ] **Step 1: 写失败测试**

Create `backend/tests/test_auth.py`:

```python
from app.common.auth import create_access_token, create_refresh_token, decode_token


def test_access_token_roundtrip():
    token = create_access_token(user_id=42)
    payload = decode_token(token)
    assert payload["sub"] == "42"
    assert payload["type"] == "access"


def test_refresh_token_roundtrip():
    token = create_refresh_token(user_id=42)
    payload = decode_token(token)
    assert payload["sub"] == "42"
    assert payload["type"] == "refresh"


def test_invalid_token_returns_none():
    assert decode_token("garbage.token.value") is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_auth.py -v`
Expected: FAIL（`ModuleNotFoundError: app.common.auth`）

- [ ] **Step 3: 实现 JWT 工具**

Create `backend/app/common/auth.py`:

```python
from datetime import datetime, timedelta
from jose import jwt, JWTError
from app.config import settings


def _create_token(user_id: int, token_type: str, expire_days: int) -> str:
    payload = {
        "sub": str(user_id),
        "type": token_type,
        "exp": datetime.utcnow() + timedelta(days=expire_days),
    }
    return jwt.encode(payload, settings.jwt_secret, algorithm=settings.jwt_algorithm)


def create_access_token(user_id: int) -> str:
    return _create_token(user_id, "access", settings.access_token_expire_days)


def create_refresh_token(user_id: int) -> str:
    return _create_token(user_id, "refresh", settings.refresh_token_expire_days)


def decode_token(token: str) -> dict | None:
    try:
        return jwt.decode(token, settings.jwt_secret, algorithms=[settings.jwt_algorithm])
    except JWTError:
        return None
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_auth.py -v`
Expected: PASS（3 项全过）

- [ ] **Step 5: 提交**

```bash
git add backend/app/common/auth.py backend/tests/test_auth.py
git commit -m "feat(backend): JWT 签发与校验工具"
```

---

### Task 6: 微信登录 service

**Files:**
- Create: `backend/app/modules/user/schemas.py`
- Create: `backend/app/modules/user/service.py`
- Test: `backend/tests/test_user_login.py`（追加）
- Modify: `backend/tests/conftest.py`（加 DB fixture）

- [ ] **Step 1: 加 DB fixture（SQLite 内存库）**

Modify `backend/tests/conftest.py`:

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.common.database import Base, get_db
from app.modules.user.models import User  # noqa: F401 注册模型


@pytest.fixture
def db_session():
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    Base.metadata.create_all(engine)
    TestingSession = sessionmaker(bind=engine, autoflush=False, autocommit=False)
    session = TestingSession()
    try:
        yield session
    finally:
        session.close()


@pytest.fixture
def client(db_session):
    app.dependency_overrides[get_db] = lambda: db_session
    yield TestClient(app)
    app.dependency_overrides.clear()
```

- [ ] **Step 2: 写失败测试（mock 微信接口）**

追加到 `backend/tests/test_user_login.py`:

```python
from app.modules.user import service
from app.modules.user.models import User


def test_login_creates_new_user(db_session, mocker):
    mocker.patch.object(
        service, "wechat_code2session", return_value={"openid": "oNEW123", "unionid": None}
    )
    result = service.login(db_session, code="any_code")
    assert result["access_token"]
    assert result["refresh_token"]
    user = db_session.query(User).filter_by(openid="oNEW123").first()
    assert user is not None


def test_login_reuses_existing_user(db_session, mocker):
    db_session.add(User(openid="oOLD999"))
    db_session.commit()
    mocker.patch.object(
        service, "wechat_code2session", return_value={"openid": "oOLD999", "unionid": None}
    )
    service.login(db_session, code="any_code")
    count = db_session.query(User).filter_by(openid="oOLD999").count()
    assert count == 1
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_user_login.py -v`
Expected: FAIL（`AttributeError: module ... has no attribute 'wechat_code2session'`）

- [ ] **Step 4: 实现 schemas**

Create `backend/app/modules/user/schemas.py`:

```python
from pydantic import BaseModel


class LoginRequest(BaseModel):
    code: str


class LoginResponse(BaseModel):
    access_token: str
    refresh_token: str
```

- [ ] **Step 5: 实现 service**

Create `backend/app/modules/user/service.py`:

```python
import httpx
from sqlalchemy.orm import Session
from app.config import settings
from app.common.exceptions import BusinessError
from app.common.auth import create_access_token, create_refresh_token
from app.modules.user.models import User

WECHAT_URL = "https://api.weixin.qq.com/sns/jscode2session"


def wechat_code2session(code: str) -> dict:
    params = {
        "appid": settings.wechat_appid,
        "secret": settings.wechat_secret,
        "js_code": code,
        "grant_type": "authorization_code",
    }
    resp = httpx.get(WECHAT_URL, params=params, timeout=5.0)
    data = resp.json()
    if "openid" not in data:
        raise BusinessError(40001, "微信登录失败")
    return data


def login(db: Session, code: str) -> dict:
    session_data = wechat_code2session(code)
    openid = session_data["openid"]
    user = db.query(User).filter_by(openid=openid).first()
    if user is None:
        user = User(openid=openid, unionid=session_data.get("unionid"))
        db.add(user)
        db.commit()
        db.refresh(user)
    return {
        "access_token": create_access_token(user.id),
        "refresh_token": create_refresh_token(user.id),
    }
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_user_login.py -v`
Expected: PASS（模型测试 + 2 个登录测试全过）

- [ ] **Step 7: 提交**

```bash
git add backend/app/modules/user backend/tests/test_user_login.py backend/tests/conftest.py
git commit -m "feat(backend): 微信登录 service（code2session + 用户 upsert + 签发 token）"
```

---

### Task 7: 鉴权依赖 get_current_user

**Files:**
- Modify: `backend/app/common/auth.py`（追加依赖）
- Test: `backend/tests/test_auth.py`（追加）

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_auth.py`:

```python
import pytest
from fastapi import HTTPException
from app.common.auth import get_current_user_id


def test_get_current_user_id_valid():
    token = create_access_token(user_id=7)
    assert get_current_user_id(authorization=f"Bearer {token}") == 7


def test_get_current_user_id_missing_header():
    with pytest.raises(HTTPException) as e:
        get_current_user_id(authorization=None)
    assert e.value.status_code == 401


def test_get_current_user_id_refresh_token_rejected():
    token = create_refresh_token(user_id=7)
    with pytest.raises(HTTPException) as e:
        get_current_user_id(authorization=f"Bearer {token}")
    assert e.value.status_code == 401
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_auth.py -v`
Expected: FAIL（`ImportError: cannot import name 'get_current_user_id'`）

- [ ] **Step 3: 实现鉴权依赖**

追加到 `backend/app/common/auth.py`:

```python
from fastapi import Header, HTTPException


def get_current_user_id(authorization: str | None = Header(default=None)) -> int:
    if not authorization or not authorization.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="缺少认证信息")
    token = authorization[len("Bearer "):]
    payload = decode_token(token)
    if payload is None or payload.get("type") != "access":
        raise HTTPException(status_code=401, detail="认证失效")
    return int(payload["sub"])
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && python -m pytest tests/test_auth.py -v`
Expected: PASS（6 项全过）

- [ ] **Step 5: 提交**

```bash
git add backend/app/common/auth.py backend/tests/test_auth.py
git commit -m "feat(backend): get_current_user_id 鉴权依赖"
```

---

### Task 8: 用户路由与全链路集成

**Files:**
- Create: `backend/app/modules/user/router.py`
- Modify: `backend/app/main.py`（注册路由）
- Test: `backend/tests/test_user_login.py`（追加 HTTP 层测试）

- [ ] **Step 1: 写失败测试（HTTP 层）**

追加到 `backend/tests/test_user_login.py`:

```python
def test_login_endpoint(client, mocker):
    mocker.patch.object(
        service, "wechat_code2session", return_value={"openid": "oHTTP1", "unionid": None}
    )
    resp = client.post("/api/v1/user/auth/login", json={"code": "test_code"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["code"] == 0
    assert body["data"]["access_token"]


def test_me_endpoint_requires_auth(client):
    resp = client.get("/api/v1/user/me")
    assert resp.status_code == 401
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && python -m pytest tests/test_user_login.py -v`
Expected: FAIL（404，路由未注册）

- [ ] **Step 3: 实现路由**

Create `backend/app/modules/user/router.py`:

```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.common.database import get_db
from app.common.response import success
from app.common.auth import get_current_user_id
from app.modules.user import service
from app.modules.user.schemas import LoginRequest

router = APIRouter(prefix="/api/v1/user", tags=["user"])


@router.post("/auth/login")
def login(req: LoginRequest, db: Session = Depends(get_db)):
    result = service.login(db, code=req.code)
    return success(result)


@router.get("/me")
def me(user_id: int = Depends(get_current_user_id)):
    return success({"user_id": user_id})
```

- [ ] **Step 4: 注册路由到 main.py**

Modify `backend/app/main.py`:

```python
from fastapi import FastAPI
from app.common.exceptions import register_exception_handlers
from app.modules.user.router import router as user_router

app = FastAPI(title="AI健康生活助手 API")
register_exception_handlers(app)
app.include_router(user_router)


@app.get("/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 5: 运行全部测试确认通过**

Run: `cd backend && python -m pytest tests/ -v`
Expected: PASS（所有测试全过）

- [ ] **Step 6: 提交**

```bash
git add backend/app/modules/user/router.py backend/app/main.py backend/tests/test_user_login.py
git commit -m "feat(backend): 用户路由（登录 + me）与全链路打通"
```

---

### Task 9: Docker Compose 与 Nginx

**Files:**
- Create: `backend/Dockerfile`
- Create: `docker/docker-compose.yml`
- Create: `docker/nginx/nginx.conf`

- [ ] **Step 1: 写后端 Dockerfile**

Create `backend/Dockerfile`:

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 2: 写 docker-compose**

Create `docker/docker-compose.yml`:

```yaml
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: meishi
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

  redis:
    image: redis:7.0
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

  backend:
    build:
      context: ../backend
    env_file:
      - ../backend/.env
    depends_on:
      - mysql
      - redis
    ports:
      - "8000:8000"

  nginx:
    image: nginx:1.24
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  mysql_data:
  redis_data:
```

- [ ] **Step 3: 写 nginx 配置**

Create `docker/nginx/nginx.conf`:

```nginx
events {}
http {
    server {
        listen 80;
        location / {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
```

- [ ] **Step 4: 人工验证（需本机装 Docker）**

Run:
```bash
cp backend/.env.example backend/.env
cd docker && docker compose up -d --build
```
等待容器起来后：
```bash
curl http://localhost/health
```
Expected: `{"status":"ok"}`

执行迁移建表：
```bash
docker compose exec backend alembic upgrade head
```
Expected: 迁移成功，users 表创建

- [ ] **Step 5: 提交**

```bash
git add backend/Dockerfile docker/
git commit -m "feat(deploy): Docker Compose（mysql/redis/backend/nginx）与 Nginx 反代"
```

---

## Self-Review 记录

- **Spec 覆盖:** 后端脚手架 ✓(Task0)、统一响应 ✓(Task1)、DB+User ✓(Task2)、迁移 ✓(Task3)、Redis ✓(Task4)、JWT ✓(Task5/7)、微信登录 ✓(Task6)、路由打通 ✓(Task8)、Docker ✓(Task9)。
- **类型一致性:** `wechat_code2session`、`login`、`create_access_token`、`decode_token`、`get_current_user_id` 在定义与调用处签名一致。
- **范围:** 前端 Taro 脚手架不在本计划，是下一份独立计划。
- **占位符:** 无 TBD/TODO，每步含完整代码与命令。
```
