# 前端 Taro 脚手架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建可在微信开发者工具中运行的 Taro 小程序前端骨架，打通微信登录链路 + JWT token 自动刷新 + 网络请求封装，并为后端补一个 token 刷新接口。

**Architecture:** 后端先用 TDD 补 `POST /auth/refresh` 接口（复用现有 JWT 工具）。前端用 Taro CLI 创建 React+TS 项目，引 NutUI，建 2 个 Tab 页（首页/我的），核心是 `services/request.ts`（请求封装 + token 注入 + 401 自动刷新）和 `store/auth.tsx`（登录态 Context）。前端不引第三方状态管理，不写单元测试（靠微信开发者工具手动验证）。

**Tech Stack:** 后端 FastAPI + python-jose + pytest；前端 Taro 4.x + React 18 + TypeScript + NutUI（@nutui/nutui-react-taro）

**设计文档:** `docs/superpowers/specs/2026-06-05-前端Taro脚手架-design.md`

**范围说明:** 仅前端脚手架 + 后端 refresh 接口。功能页（建档/记录/周报/问答）不在本计划。

**环境备注:**
- 后端 venv 在 `backend/.venv`，跑测试：在 `backend/` 目录下用 `.venv\Scripts\python.exe -m pytest tests/ -v`（Windows PowerShell）
- 前端需要 Node.js + npm。Taro CLI 用 `npm install -g @tarojs/cli` 或 `npx @tarojs/cli`
- 微信 AppID 在 `backend/.env` 里（`wx8c83a0b6a7a766e1`），前端 `project.config.json` 也要填

---

## 文件结构

```
backend/app/modules/user/
├── schemas.py        # 追加 RefreshRequest
├── service.py        # 追加 refresh()
└── router.py         # 追加 POST /auth/refresh
backend/tests/
└── test_user_login.py  # 追加 refresh 测试

frontend/
├── config/                  # Taro 构建配置（CLI 生成）
├── src/
│   ├── app.tsx              # 应用入口（包 AuthProvider + 启动恢复登录态）
│   ├── app.config.ts        # tabBar 2 个 Tab + 页面注册
│   ├── pages/
│   │   ├── index/           # 首页
│   │   │   ├── index.tsx
│   │   │   └── index.config.ts
│   │   └── profile/         # 我的
│   │       ├── profile.tsx
│   │       └── profile.config.ts
│   ├── services/
│   │   ├── request.ts       # 请求封装（核心）
│   │   └── user.ts          # 用户 API
│   ├── store/
│   │   └── auth.tsx         # AuthContext
│   ├── utils/
│   │   └── storage.ts       # 本地存储封装
│   └── constants/
│       └── config.ts        # baseURL 等常量
├── package.json
├── tsconfig.json
└── project.config.json
```

---

### Task 1: 后端 token 刷新接口

**Files:**
- Modify: `backend/app/modules/user/schemas.py`
- Modify: `backend/app/modules/user/service.py`
- Modify: `backend/app/modules/user/router.py`
- Test: `backend/tests/test_user_login.py`（追加）

- [ ] **Step 1: 写失败测试（service 层）**

追加到 `backend/tests/test_user_login.py` 末尾（文件顶部已有 `from app.common.auth import create_access_token` 和 `from app.modules.user import service`，还需用到 `create_refresh_token`，在顶部 import 行追加）：

先把顶部第一行改为：
```python
from app.common.auth import create_access_token, create_refresh_token
```

再在文件末尾追加：
```python
def test_refresh_success():
    refresh_token = create_refresh_token(user_id=7)
    result = service.refresh(refresh_token)
    assert result["access_token"]
    assert result["refresh_token"]


def test_refresh_rejects_access_token():
    from app.common.exceptions import BusinessError
    access_token = create_access_token(user_id=7)
    try:
        service.refresh(access_token)
        assert False, "应抛 BusinessError"
    except BusinessError as e:
        assert e.code == 40101


def test_refresh_rejects_garbage():
    from app.common.exceptions import BusinessError
    try:
        service.refresh("garbage.token.value")
        assert False, "应抛 BusinessError"
    except BusinessError as e:
        assert e.code == 40101
```

- [ ] **Step 2: 运行测试确认失败**

Run（在 `backend/` 目录）: `.venv\Scripts\python.exe -m pytest tests/test_user_login.py -v`
Expected: FAIL（`AttributeError: module ... has no attribute 'refresh'`）

- [ ] **Step 3: 实现 service.refresh**

在 `backend/app/modules/user/service.py` 顶部 import 区，把第 5 行改为同时引入 `decode_token`：
```python
from app.common.auth import create_access_token, create_refresh_token, decode_token
```

在文件末尾追加：
```python
def refresh(refresh_token: str) -> dict:
    payload = decode_token(refresh_token)
    if payload is None or payload.get("type") != "refresh":
        raise BusinessError(40101, "登录已过期")
    user_id = int(payload["sub"])
    return {
        "access_token": create_access_token(user_id),
        "refresh_token": create_refresh_token(user_id),
    }
```

- [ ] **Step 4: 加 RefreshRequest schema**

在 `backend/app/modules/user/schemas.py` 末尾追加：
```python
class RefreshRequest(BaseModel):
    refresh_token: str
```

- [ ] **Step 5: 加路由**

在 `backend/app/modules/user/router.py`：把第 7 行 import 改为同时引入 `RefreshRequest`：
```python
from app.modules.user.schemas import LoginRequest, RefreshRequest
```

在 `me` 路由之前（`login` 路由之后）追加：
```python
@router.post("/auth/refresh")
def refresh(req: RefreshRequest):
    result = service.refresh(req.refresh_token)
    return success(result)
```

- [ ] **Step 6: 运行全部测试确认通过**

Run（在 `backend/` 目录）: `.venv\Scripts\python.exe -m pytest tests/ -v`
Expected: PASS（原 16 + 新 3 = 19 个测试全过）

- [ ] **Step 7: 加 HTTP 层测试**

追加到 `backend/tests/test_user_login.py` 末尾：
```python
def test_refresh_endpoint(client):
    refresh_token = create_refresh_token(user_id=7)
    resp = client.post("/api/v1/user/auth/refresh", json={"refresh_token": refresh_token})
    assert resp.status_code == 200
    body = resp.json()
    assert body["code"] == 0
    assert body["data"]["access_token"]
    assert body["data"]["refresh_token"]


def test_refresh_endpoint_rejects_garbage(client):
    resp = client.post("/api/v1/user/auth/refresh", json={"refresh_token": "garbage"})
    assert resp.status_code == 200
    body = resp.json()
    assert body["code"] == 40101
```

- [ ] **Step 8: 运行确认通过**

Run（在 `backend/` 目录）: `.venv\Scripts\python.exe -m pytest tests/ -v`
Expected: PASS（21 个测试全过）

- [ ] **Step 9: 提交**

```bash
git add backend/app/modules/user backend/tests/test_user_login.py
git commit -m "feat(backend): token 刷新接口 POST /auth/refresh"
```

---

### Task 2: Taro 项目初始化

**Files:**
- Create: `frontend/`（由 Taro CLI 生成整套）

- [ ] **Step 1: 用 Taro CLI 创建项目**

Run（在仓库根目录 `D:\wwwroot\testing\meishi`）:
```bash
npx @tarojs/cli@latest init frontend
```
交互选项按以下选择：
- 项目名称: `frontend`（如已自动填则确认）
- 框架: **React**
- 是否需要 TypeScript: **是**
- CSS 预处理器: **Sass**
- 包管理器: **npm**
- 编译工具: **Webpack5**（默认即可）
- 模板: **默认模板（default）**

若 `npx` 交互不便，可先全局装：`npm install -g @tarojs/cli` 后用 `taro init frontend`。

Expected: 生成 `frontend/` 目录，含 `package.json`、`config/`、`src/`、`project.config.json`、`tsconfig.json`。

- [ ] **Step 2: 安装依赖**

Run（在 `frontend/` 目录）: `npm install`
Expected: `node_modules/` 安装成功，无致命报错。

- [ ] **Step 3: 确认能编译微信小程序**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 生成 `dist/` 目录，编译成功无报错。
（注：`.gitignore` 已忽略 `node_modules/` 和 `dist/`）

- [ ] **Step 4: 填入微信 AppID**

Modify `frontend/project.config.json`：把 `"appid"` 字段的值改为 `wx8c83a0b6a7a766e1`。

- [ ] **Step 5: 提交**

```bash
git add frontend/
git commit -m "feat(frontend): Taro 项目初始化（React + TS + Sass）"
```

---

### Task 3: 引入 NutUI + 常量与存储工具

**Files:**
- Modify: `frontend/package.json`（装 NutUI）
- Modify: `frontend/config/index.ts`（NutUI 按需配置，如模板需要）
- Create: `frontend/src/constants/config.ts`
- Create: `frontend/src/utils/storage.ts`

- [ ] **Step 1: 安装 NutUI**

Run（在 `frontend/` 目录）:
```bash
npm install @nutui/nutui-react-taro
```
Expected: 安装成功。

- [ ] **Step 2: 写常量文件**

Create `frontend/src/constants/config.ts`:
```typescript
// 后端 API 基础地址。本地调试时指向本机后端；上线改为线上域名。
// 微信开发者工具需在「详情 → 本地设置」勾选「不校验合法域名」才能用 localhost/IP。
export const API_BASE_URL = 'http://localhost:8000'

// 本地存储 key
export const STORAGE_KEYS = {
  ACCESS_TOKEN: 'access_token',
  REFRESH_TOKEN: 'refresh_token',
} as const
```

- [ ] **Step 3: 写存储工具**

Create `frontend/src/utils/storage.ts`:
```typescript
import Taro from '@tarojs/taro'
import { STORAGE_KEYS } from '../constants/config'

export function getAccessToken(): string {
  return Taro.getStorageSync(STORAGE_KEYS.ACCESS_TOKEN) || ''
}

export function getRefreshToken(): string {
  return Taro.getStorageSync(STORAGE_KEYS.REFRESH_TOKEN) || ''
}

export function setTokens(accessToken: string, refreshToken: string): void {
  Taro.setStorageSync(STORAGE_KEYS.ACCESS_TOKEN, accessToken)
  Taro.setStorageSync(STORAGE_KEYS.REFRESH_TOKEN, refreshToken)
}

export function clearTokens(): void {
  Taro.removeStorageSync(STORAGE_KEYS.ACCESS_TOKEN)
  Taro.removeStorageSync(STORAGE_KEYS.REFRESH_TOKEN)
}
```

- [ ] **Step 4: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功（这两个文件还没被引用，只验证语法不报错）。

- [ ] **Step 5: 提交**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/constants frontend/src/utils
git commit -m "feat(frontend): 引入 NutUI + 常量与存储工具"
```

---

### Task 4: 网络请求封装

**Files:**
- Create: `frontend/src/services/request.ts`

依赖说明：本任务实现 `request()`，其中 401 刷新逻辑需要调用后端 `/auth/refresh`。为避免与 `user.ts` 循环依赖，刷新请求在 `request.ts` 内用裸 `Taro.request` 直接发，不经过 `user.ts`。

- [ ] **Step 1: 写请求封装**

Create `frontend/src/services/request.ts`:
```typescript
import Taro from '@tarojs/taro'
import { API_BASE_URL } from '../constants/config'
import { getAccessToken, getRefreshToken, setTokens, clearTokens } from '../utils/storage'

interface RequestOptions {
  url: string
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE'
  data?: Record<string, any>
}

// 后端统一响应结构
interface ApiResponse<T> {
  code: number
  message: string
  data: T
  timestamp: number
}

// 防止刷新风暴：并发 401 时只发起一次刷新，其余请求等同一个 Promise
let refreshing: Promise<boolean> | null = null

// 用裸 Taro.request 调刷新接口，避免经过 request() 造成递归
async function doRefresh(): Promise<boolean> {
  const refreshToken = getRefreshToken()
  if (!refreshToken) return false
  try {
    const res = await Taro.request({
      url: `${API_BASE_URL}/api/v1/user/auth/refresh`,
      method: 'POST',
      data: { refresh_token: refreshToken },
      header: { 'Content-Type': 'application/json' },
    })
    const body = res.data as ApiResponse<{ access_token: string; refresh_token: string }>
    if (res.statusCode === 200 && body.code === 0) {
      setTokens(body.data.access_token, body.data.refresh_token)
      return true
    }
    return false
  } catch {
    return false
  }
}

async function ensureRefreshed(): Promise<boolean> {
  if (!refreshing) {
    refreshing = doRefresh().finally(() => {
      refreshing = null
    })
  }
  return refreshing
}

export async function request<T = any>(options: RequestOptions): Promise<T> {
  const send = async (): Promise<Taro.request.SuccessCallbackResult> => {
    const token = getAccessToken()
    return Taro.request({
      url: `${API_BASE_URL}${options.url}`,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Content-Type': 'application/json',
        ...(token ? { Authorization: `Bearer ${token}` } : {}),
      },
    })
  }

  let res: Taro.request.SuccessCallbackResult
  try {
    res = await send()
  } catch {
    Taro.showToast({ title: '网络异常', icon: 'none' })
    throw new Error('network error')
  }

  // 401：尝试刷新后重发一次
  if (res.statusCode === 401) {
    const ok = await ensureRefreshed()
    if (!ok) {
      clearTokens()
      Taro.showToast({ title: '请重新登录', icon: 'none' })
      throw new Error('unauthorized')
    }
    try {
      res = await send()
    } catch {
      Taro.showToast({ title: '网络异常', icon: 'none' })
      throw new Error('network error')
    }
  }

  const body = res.data as ApiResponse<T>
  if (body.code !== 0) {
    Taro.showToast({ title: body.message || '请求失败', icon: 'none' })
    throw new Error(body.message || 'business error')
  }
  return body.data
}
```

- [ ] **Step 2: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功，无 TS 类型错误。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/services/request.ts
git commit -m "feat(frontend): 网络请求封装（token 注入 + 401 自动刷新）"
```

---

### Task 5: 用户 API service

**Files:**
- Create: `frontend/src/services/user.ts`

- [ ] **Step 1: 写用户 API**

Create `frontend/src/services/user.ts`:
```typescript
import Taro from '@tarojs/taro'
import { request } from './request'

interface LoginResult {
  access_token: string
  refresh_token: string
}

interface MeResult {
  user_id: number
}

// 用微信 code 换 token
export async function login(code: string): Promise<LoginResult> {
  return request<LoginResult>({
    url: '/api/v1/user/auth/login',
    method: 'POST',
    data: { code },
  })
}

// 获取当前登录用户信息（带 token，验证登录态）
export async function getMe(): Promise<MeResult> {
  return request<MeResult>({
    url: '/api/v1/user/me',
    method: 'GET',
  })
}

// 调微信 wx.login 拿 code（静默，不需要用户授权）
export async function wxLoginCode(): Promise<string> {
  const res = await Taro.login()
  if (!res.code) throw new Error('wx.login 失败')
  return res.code
}
```

- [ ] **Step 2: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/services/user.ts
git commit -m "feat(frontend): 用户 API service（login/getMe/wxLoginCode）"
```

---

### Task 6: 登录态 Context（AuthContext）

**Files:**
- Create: `frontend/src/store/auth.tsx`

- [ ] **Step 1: 写 AuthContext**

Create `frontend/src/store/auth.tsx`:
```typescript
import { createContext, useContext, useState, useCallback, ReactNode } from 'react'
import { setTokens, clearTokens, getAccessToken } from '../utils/storage'
import { login as apiLogin, getMe, wxLoginCode } from '../services/user'

interface AuthState {
  isLoggedIn: boolean
  userId: number | null
  // 首次登录：wx.login → 换 token → 存储 → 拉 /me
  loginWithWechat: () => Promise<void>
  // 启动时恢复登录态：有 token 就拉 /me 验证
  restore: () => Promise<void>
  logout: () => void
}

const AuthContext = createContext<AuthState | null>(null)

export function AuthProvider({ children }: { children: ReactNode }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false)
  const [userId, setUserId] = useState<number | null>(null)

  const loginWithWechat = useCallback(async () => {
    const code = await wxLoginCode()
    const tokens = await apiLogin(code)
    setTokens(tokens.access_token, tokens.refresh_token)
    const me = await getMe()
    setUserId(me.user_id)
    setIsLoggedIn(true)
  }, [])

  const restore = useCallback(async () => {
    if (!getAccessToken()) return
    try {
      const me = await getMe()
      setUserId(me.user_id)
      setIsLoggedIn(true)
    } catch {
      // getMe 内部已处理 401 刷新；走到这里说明刷新也失败，保持未登录
      setIsLoggedIn(false)
      setUserId(null)
    }
  }, [])

  const logout = useCallback(() => {
    clearTokens()
    setIsLoggedIn(false)
    setUserId(null)
  }, [])

  return (
    <AuthContext.Provider value={{ isLoggedIn, userId, loginWithWechat, restore, logout }}>
      {children}
    </AuthContext.Provider>
  )
}

export function useAuth(): AuthState {
  const ctx = useContext(AuthContext)
  if (!ctx) throw new Error('useAuth 必须在 AuthProvider 内使用')
  return ctx
}
```

- [ ] **Step 2: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/store/auth.tsx
git commit -m "feat(frontend): 登录态 Context（AuthProvider + useAuth）"
```

---

### Task 7: 页面（首页 + 我的）与 tabBar 配置

**Files:**
- Create: `frontend/src/pages/index/index.tsx`
- Create: `frontend/src/pages/index/index.config.ts`
- Create: `frontend/src/pages/profile/profile.tsx`
- Create: `frontend/src/pages/profile/profile.config.ts`
- Modify: `frontend/src/app.config.ts`

注意：Taro CLI 默认模板已生成 `src/pages/index/`。本任务覆盖其内容并新增 profile 页。

- [ ] **Step 1: 写首页**

Create/覆盖 `frontend/src/pages/index/index.tsx`:
```tsx
import { View, Text } from '@tarojs/components'
import { useAuth } from '../../store/auth'
import './index.scss'

export default function Index() {
  const { isLoggedIn, userId } = useAuth()
  return (
    <View className="index">
      <Text className="title">AI 健康生活助手</Text>
      <Text className="status">
        {isLoggedIn ? `已登录（user_id: ${userId}）` : '未登录'}
      </Text>
    </View>
  )
}
```

Create `frontend/src/pages/index/index.config.ts`:
```typescript
export default {
  navigationBarTitleText: '首页',
}
```

若 CLI 模板生成了 `index.scss`，保留；若没有，Create `frontend/src/pages/index/index.scss`:
```scss
.index {
  padding: 40px 24px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.title {
  font-size: 36px;
  font-weight: bold;
}
.status {
  font-size: 28px;
  color: #888;
}
```

- [ ] **Step 2: 写「我的」页**

Create `frontend/src/pages/profile/profile.tsx`:
```tsx
import { View, Text } from '@tarojs/components'
import { Button } from '@nutui/nutui-react-taro'
import Taro from '@tarojs/taro'
import { useAuth } from '../../store/auth'
import './profile.scss'

export default function Profile() {
  const { isLoggedIn, userId, loginWithWechat, logout } = useAuth()

  const handleLogin = async () => {
    try {
      await loginWithWechat()
      Taro.showToast({ title: '登录成功', icon: 'success' })
    } catch {
      Taro.showToast({ title: '登录失败', icon: 'none' })
    }
  }

  return (
    <View className="profile">
      {isLoggedIn ? (
        <>
          <Text className="info">user_id: {userId}</Text>
          <Button type="default" onClick={logout}>退出登录</Button>
        </>
      ) : (
        <Button type="primary" onClick={handleLogin}>微信登录</Button>
      )}
    </View>
  )
}
```

Create `frontend/src/pages/profile/profile.config.ts`:
```typescript
export default {
  navigationBarTitleText: '我的',
}
```

Create `frontend/src/pages/profile/profile.scss`:
```scss
.profile {
  padding: 40px 24px;
  display: flex;
  flex-direction: column;
  gap: 24px;
  align-items: center;
}
.info {
  font-size: 28px;
}
```

- [ ] **Step 3: 配置 tabBar 与页面注册**

覆盖 `frontend/src/app.config.ts`:
```typescript
export default defineAppConfig({
  pages: [
    'pages/index/index',
    'pages/profile/profile',
  ],
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#fff',
    navigationBarTitleText: 'AI 健康生活助手',
    navigationBarTextStyle: 'black',
  },
  tabBar: {
    color: '#999',
    selectedColor: '#1989fa',
    backgroundColor: '#fff',
    list: [
      { pagePath: 'pages/index/index', text: '首页' },
      { pagePath: 'pages/profile/profile', text: '我的' },
    ],
  },
})
```

注意：Taro tabBar 默认要求每个 tab 有 iconPath/selectedIconPath，但微信小程序允许不带图标（纯文字）。若编译或开发者工具报 tabBar 图标错误，则本步加一行说明：MVP 阶段先不放图标，开发者工具中纯文字 tabBar 可正常显示；如确需图标，后续放 `src/assets/` 再补 `iconPath`。

- [ ] **Step 4: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功，`dist/` 中含 index 和 profile 两个页面。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages frontend/src/app.config.ts
git commit -m "feat(frontend): 首页 + 我的页 + tabBar 配置"
```

---

### Task 8: 应用入口接入 AuthProvider 与启动恢复登录态

**Files:**
- Modify: `frontend/src/app.tsx`

- [ ] **Step 1: 改写 app.tsx**

覆盖 `frontend/src/app.tsx`（保留 CLI 生成的全局样式 import，如 `import './app.scss'`；若模板里全局样式文件名不同，按实际调整）:
```tsx
import { PropsWithChildren } from 'react'
import { useLaunch } from '@tarojs/taro'
import { AuthProvider, useAuth } from './store/auth'
import './app.scss'

function LaunchRestore({ children }: PropsWithChildren) {
  const { restore } = useAuth()
  useLaunch(() => {
    restore()
  })
  return <>{children}</>
}

function App({ children }: PropsWithChildren) {
  return (
    <AuthProvider>
      <LaunchRestore>{children}</LaunchRestore>
    </AuthProvider>
  )
}

export default App
```

说明：`useLaunch` 在小程序启动时触发一次，调用 `restore()` 读本地 token 并验证登录态。`restore()` 内部失败时静默保持未登录。

- [ ] **Step 2: 验证编译**

Run（在 `frontend/` 目录）: `npm run build:weapp`
Expected: 编译成功。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/app.tsx
git commit -m "feat(frontend): app 入口接入 AuthProvider + 启动恢复登录态"
```

---

### Task 9: 微信开发者工具手动验证

**Files:** 无（纯手动验证 + 记录结果）

前置：本机已装微信开发者工具，后端在跑（`docker compose up` 或本机 `uvicorn`，且 `.env` 填了真实 AppID/Secret），后端可访问 `http://localhost:8000`。

- [ ] **Step 1: 启动后端**

二选一：
- Docker：仓库根目录下 `cd docker && docker compose up -d`（需 MySQL/Redis 起来）
- 或本机：`backend/` 目录下 `.venv\Scripts\python.exe -m uvicorn app.main:app --reload`（需本机有可连的 MySQL；若无，登录会因写库失败，可临时把 service.login 的 DB 写入注释掉验证链路——但不提交此改动）

验证：浏览器访问 `http://localhost:8000/health` 返回 `{"status":"ok"}`。

- [ ] **Step 2: 用微信开发者工具打开 frontend**

- 打开微信开发者工具 → 导入项目 → 选 `frontend/` 目录 → AppID 填 `wx8c83a0b6a7a766e1`
- 「详情 → 本地设置」勾选「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」（否则 localhost 请求被拦）

- [ ] **Step 3: 验证登录链路**

- 底部应有「首页 / 我的」两个 Tab，首页显示「未登录」
- 进「我的」→ 点「微信登录」→ 应弹「登录成功」
- 回首页 → 应显示「已登录（user_id: N）」
- 验证 token：开发者工具 → Storage 面板，应能看到 `access_token` 和 `refresh_token`

- [ ] **Step 4: 验证启动恢复登录态**

- 登录后，点开发者工具「编译」重启小程序 → 首页应直接显示「已登录」（说明 restore 生效）

- [ ] **Step 5: 验证 token 自动刷新（可选，较难手动触发）**

- 在 Storage 面板把 `access_token` 改成一个无效值（如 `bad`），保留 `refresh_token`
- 重新编译 → restore 调 /me 会 401 → 自动用 refresh_token 刷新 → 应仍显示「已登录」
- 若刷新也失败（把 refresh_token 也改坏）→ 应显示「未登录」

- [ ] **Step 6: 记录验证结果**

把验证通过情况记录到 PROGRESS.md（在 finishing 阶段统一更新）。若某步失败，回到对应 Task 修复。

---

## Self-Review 记录

- **Spec 覆盖:** 后端 refresh 接口 ✓(Task1)、Taro 初始化 ✓(Task2)、NutUI+常量+存储 ✓(Task3)、请求封装 ✓(Task4)、用户 API ✓(Task5)、AuthContext ✓(Task6)、2 个 Tab 页 ✓(Task7)、app 入口+启动恢复 ✓(Task8)、手动验证 ✓(Task9)。
- **类型一致性:** `request()`、`login()`、`getMe()`、`refresh()`、`loginWithWechat()`、`restore()`、`useAuth()`、`setTokens/getAccessToken/getRefreshToken/clearTokens` 在定义与调用处签名一致。后端 `service.refresh`、`RefreshRequest`、路由 `/auth/refresh` 一致。
- **循环依赖:** request.ts 的刷新用裸 Taro.request，不依赖 user.ts，避免 request↔user 循环。
- **占位符:** 无 TBD/TODO，每步含完整代码或命令。
- **范围:** 功能页（建档/记录/周报/问答）不在本计划，留待后续子项目。
